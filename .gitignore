# ESP-IDF build output
/build/
# Leak-test variant build (sdkconfig.ci overlay) — see PLAN §7A.7
/build_leak/
/sdkconfig.leak

# Timestamped backups of successful builds (tools/backup_build.sh)
/build_backups/

# Generated config (regenerated from sdkconfig.defaults)
/sdkconfig
/sdkconfig.old

# Component manager downloads
/managed_components/
dependencies.lock

# macOS
.DS_Store
